## Installation

1. npm install 
2. npm run dev // for run frontEnd
3. npm create vite@latest
4. dotnet add package Microsoft.EntityFrameworkCore.SqlServer
5. dotnet tool install --global dotnet-ef
6. dotnet ef database update
7. dotnet run // For Run backEnd
